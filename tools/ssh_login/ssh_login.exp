#!/usr/bin/expect

set host [lindex $argv 0]
set username [lindex $argv 1]
set password [lindex $argv 2]

set timeout 30

spawn ssh $username@$host

expect "*password:"

send "$password\r"

expect "ubuntu@*"

send "sudo su\r"

expect "*password*"

send "$password\r"

interact
